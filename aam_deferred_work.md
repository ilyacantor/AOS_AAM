# Deferred work — aam

1. 2026-04-15 | wild-splashing-simon | tests/playwright/controls-fixes.spec.js:91 | F3 asserts `#btn-run-inference` has count 0, but T1 in aam-topology-fixes.spec.js:13 asserts it must be count 1 and enabled. The served HTML has both `id="btn-run-inference"` and `data-testid="btn-run-inference"` — T1 matches current new-architecture spec, F3 is stale. Fix is to drop `#btn-run-inference` from F3's absence list. Not touched in current session because scope is auto-fetch handoff only; flagging so the next session can reconcile the two specs. | severity: cosmetic | blocking: full playwright suite green RESOLVED 2026-05-12 (dropped `#btn-run-inference` from F3 absence list per the recommended fix; full Playwright suite now 28/28 green)
2. 2026-04-15 | wild-splashing-simon | tests/playwright/controls-fixes.spec.js:7 | F1 ("Triple Health panel shows AAM triples > 0 with coverage") fails because `POST /api/aam/infer` returns 200 with `pipes_created: 0`, `triple_write: null`, and all 4 AOD candidates marked `Blocked by AOD governance`. The inference endpoint works but no triples are written, so the health panel shows 0. Response also returns `entity_id: "empty-entity"` which looks suspicious — either AOD handoff isn't passing entity_id properly, or the governance gate is rejecting every candidate. Needs cross-service diagnosis (AOD governance gate + AAM inference path). Out of scope for auto-fetch handoff fix. | severity: degraded | blocking: full playwright suite green, and a real pipeline concern if triples stopped writing PARTIALLY RESOLVED 2026-05-12 (F1 now passes — Triple Health total_count > 0; underlying /api/aam/infer governance-blocked path was NOT fixed in this session, F1 currently green only because of pre-existing triples + 19 new triples from the demo ingest path. Re-open if a fresh DB ever shows F1 red again.)
3. 2026-05-12 | aam-demo-data-plumbing | app/ingest/triples.py + app/routers/ingest_demo.py | New demo path (Workato + Boomi via ipaas_stub) writes triples directly via app/db/triple_writer.py — same direct-PG write path AAM already uses. CLAUDE.md flags `direct-PG write code in AOD/AAM` as tech debt that must not be extended; the demo path was kept as a parallel triple builder rather than extending app/converters/triple_converter.py, but the underlying write still lands in DCL's `semantic_triples` from AAM (not via a DCL ingest endpoint). When DCL ships POST /api/dcl/ingest/production (WP-8 DCL-side), migrate the demo path to POST records instead of triples. | severity: cosmetic | blocking: nothing today
   RESOLVED 2026-05-14 (Plan C WP6, commit 359ee88) — `app/routers/ingest_demo.py` deleted entirely; the orchestrator is gone. Console now owns the operator-facing ingest surface and uses AAM's /api/aam/infer + WP3 resolver APIs over HTTP. No AAM code path writes synthetic FinOps demo triples anymore. The /api/aam/infer direct-PG path remains tech debt — see entry #10.
4. 2026-05-12 | aam-demo-data-plumbing | tests/test_harness.py:655,667,670,695,697,712,714 | Pre-existing TypeError: six retention / phase-test call-sites were invoking test functions without the required `run_id` argument, so `python tests/test_harness.py` died on the first call. Fixed by passing `fresh_run_id` to each. Pre-existing aod_handoff_log had 1 candidate but 0 fabric_planes, which made the 'fabric_comparison has_aod_data' assertion fail; seeded a fresh handoff (aod_discovery_id=harness-seed-001) with kong + mulesoft fabric_planes via POST /api/handoff/aod/receive. | severity: degraded | blocking: standalone harness green RESOLVED 2026-05-12 (both fixes applied; harness now 52/52 green, twice in a row identical)
5. 2026-05-12 | aam-finops-pivot | Supabase pooler `aws-1-us-east-2.pooler.supabase.com` | Under sustained Playwright load (~5+ minutes of test runs after the FinOps demo ingest landed ~210k triples), the pooler intermittently times out on /api/aam/triple-health and other queries. Manifests as P8–P12 + F1 going red mid-suite after a long warm-up; the same specs pass alone. Not caused by AAM code — the pooler latency spikes on aged connections. Workaround: run Playwright in smaller batches or restart pm2 between full-suite runs. Real fix: switch AAM's PG connection pool to use a shorter idle-timeout + pre-warm + retry on dead-connection. | severity: degraded | blocking: full Playwright suite stays green in a single long run
   RESOLVED 2026-05-14 (Plan C WP6, commit 359ee88) — moot. The ~210k-triple demo ingest path that triggered the pooler timeouts is deleted. AAM no longer pushes that volume from its own code; Farm + Convergence remain on the same pooler but generate via their own HTTP push to DCL (different connection origin). Re-open only if a future sustained-load scenario reintroduces the symptom.
6. 2026-05-13 | aam-finops-pivot | app/routers/demo.py + tests/playwright/aam-demo-experience.spec.js | FinOps pivot — replaced Customer 360 / combined-financials direction with SaaS-spending demo (NetSuite vendor master + AP invoices via Workato, Okta apps + users + assignments via Boomi). New demo question: "Show me SaaS subscriptions where actual utilization is below 50% of paid licenses, ranked by potential annual savings". Identity resolution pairs NetSuite vendor with Okta app, one match at 0.71 confidence (LinkedIn Sales Navigator ↔ LinkedIn Sales Nav.). controls-fixes F1's old invariant ("health.total_count >= ledger.total_triples") no longer holds since the demo ingest path writes triples through the ledger under source_system != 'AAM' (counted by the ledger but excluded by health's source_system='AAM' filter); test updated to assert only health > 0. | severity: cosmetic | blocking: nothing PARTIALLY RESOLVED 2026-05-14 (WP3 wired live resolver; identity_matches.json deleted; demo.py /api/aam/demo/identity-matches now reads from resolver_hitl_queue + semantic_triples canonical_id joins. Playwright spec updated to read confidence from /api/aam/demo/identity-matches at runtime instead of hardcoded 71%. Source_system='AAM' filter on /api/aam/infer is still WP4's job per WP3 prompt section 6.) RESOLVED 2026-05-14 (commit 8099cd4: demo ingest path moved to DCL HTTP — no longer writes through write_triples_with_ledger. Ledger now only tracks /api/aam/infer + drift + fabric_planes writes (all source_system='AAM'). F1 invariant rewritten as a run-scoped cross-check: health.total_count >= latest committed ledger entry's triple_count — bounded and meaningful, unlike the original which compared a bounded active-set count against unbounded historical ledger sum.)
7. 2026-05-14 | aam-resolver-wp3 | DCL semantic_triples.resolution_method CHECK constraint | DCL's schema accepts only {'deterministic','fuzzy','manual'}. AAM's resolver internally tracks a richer taxonomy (exact/alias/pattern/fuzzy/hitl_pending/hitl_confirmed/discovery/rejected). app/ingest/triples.py _RESOLUTION_METHOD_TO_PG translates at the write boundary so DCL's schema is preserved (per A10) and AAM keeps its provenance fidelity in-process. When DCL grows the constraint vocabulary (Convergence-coordination round per SCHEMA_CONTRACT.md), drop the translator and write AAM's native values through. | severity: cosmetic | blocking: nothing
8. 2026-05-14 | aam-resolver-wp3 | tests/playwright/controls-fixes.spec.js:7 F1 | F1 ("Triple Health panel shows AAM triples > 0 with coverage") fails on a fresh-DB session because /api/aam/infer governance-blocks every AOD candidate and writes 0 triples — same pre-existing root cause as entry #2. WP3 explicitly out-of-scope per WP3 prompt section 6 ("F1 — DO NOT REVERT in WP3; that's WP4's job"). | severity: degraded | blocking: full Playwright suite on a fresh DB RESOLVED 2026-05-14 (commit 8099cd4: F1 spec now POSTs /api/handoff/aod/fetch before /api/aam/infer so candidates exist on a fresh DB. Replays the saved aod_last_payload via the existing handoff replay endpoint — read-only setup under B17's allowed exception.)
9. 2026-05-14 | aam-dcl-pusher-wp4 | app/ingest/triple_builder.py _CONCEPT_TO_DCL_ROOT | AAM's FieldMappings use operator-friendly PascalCase concept names (Vendor, Customer, SaaSApp). DCL's ConceptRegistry validates against 151 lowercase ontology ids and the persona_domains.yaml subset (~37 mapped prefixes). The translator at app/ingest/triple_builder.py canonicalizes 10 PascalCase concepts to ontology-valid roots (Vendor->vendor, Customer->customer, SaaSApp->it_asset, APInvoice->invoice, User->employee, Assignment->it_asset, Transaction->revenue, Incident->support, Expense->opex, Employee->employee). Tech-debt resolution: when AAM mappings are themselves rewritten in lowercase ontology vocabulary (Platform WP-8 Semantic Field Mapper), drop the translator and let FieldMapping.concept pass through. | severity: cosmetic | blocking: nothing
   RESOLVED 2026-05-14 — MAPPINGS in app/ingest/mappings.py rewritten to lowercase canonical DCL concept IDs (customer, employee, vendor, revenue, support, opex, invoice, it_asset.saas_app, it_asset.assignment). SaaSApp and Assignment retain distinction via compound roots that share the registered "it_asset" root (DCL's prefix-based ConceptRegistry.is_valid_concept accepts any concept whose first dot-segment is registered). _CONCEPT_TO_DCL_ROOT and _canonical_concept() removed from triple_builder.py — concept now composed as `f"{m.concept}.{m.property}"` directly. demo.py SQL queries (_latest_run_for, _answer_saas_utilization) updated from `concept = 'SaaSApp'` (which never matched any rows post-WP4) to `concept LIKE 'it_asset.saas_app.%'`. End-to-end verified: 11/11 triple_builder unit tests pass, 4/4 ingest_demo_e2e tests pass, direct DCL POST with all four compound concepts accepted (triple_count=4 written, concept_summary={invoice:1, it_asset:2, vendor:1}).
10. 2026-05-14 | aam-dcl-pusher-wp4 | app/routers/ingest_demo.py orchestration / app/converters/triple_converter.py:161 | The /api/aam/ingest/demo path (Workato/Boomi vendor records) now pushes to DCL via HTTP per WP4 — entry #3 of this list is RESOLVED for that path. The /api/aam/infer path (AAM's own mapping.pipe/mapping.connection/mapping.fabric metadata triples) STILL writes direct-PG with source_system='AAM' via app/converters/triple_converter.py. That path is out of WP4 scope and remains tagged tech debt per entry #3. When DCL registers mapping.* concepts in ontology_concepts.yaml + persona_domains.yaml (Convergence-coordinated schema round), migrate /api/aam/infer to the same DCL ingest endpoint. | severity: cosmetic | blocking: nothing
11. 2026-05-14 | aam-dcl-pusher-wp4 | tests/playwright/aam-demo-experience.spec.js:39 FinOps agent test | The first test in aam-demo-experience.spec.js navigates to FINOPS_URL (default http://localhost:3001) and asserts on the FinOps UI. The FinOps app lives at /home/ilyac/code/finops and is not started by the AAM service stack — it's a separate process the operator launches via aos-launch.sh. The test fails when FinOps isn't running, but that's a cross-repo orchestration concern, not an AAM regression. Other 4 tests in the spec (Consumer View, Pipe Catalog, Semantic Mapping, Identity Resolution) all pass in isolation against AAM alone. | severity: cosmetic | blocking: full Playwright suite green requires the FinOps service running on its configured port
    RESOLVED 2026-05-14 (Plan C WP6, commit 359ee88) — `tests/playwright/aam-demo-experience.spec.js` deleted. Cross-repo orchestration concern gone. The four salvageable operator surfaces (catalog, mappings, identity, consumer) ported to Console under `console/frontend/e2e/pipelines-*.spec.ts`, exercising the same paths through Console's UI.
12. 2026-05-14 | aam-dcl-pusher-wp4 | DCLPusher serial batch dispatch | DCLPusher.push() sends batches serially. A 213k-triple push takes ~4 min through ~427 HTTP round trips at ~500ms each. Farm's equivalent (farm/src/services/dcl_triple_pusher.py) uses async batches with concurrency=2 — same total volume completes in ~2 min. The two pushers serve different volume profiles: AAM's healthy-scenario push (19 triples) doesn't need parallelism; FinOps stress runs (213k) do. When AAM's demo expands to production-scale ingest, port Farm's async pattern (httpx.AsyncClient + asyncio.Semaphore). For now the serial path keeps the dcl_pusher.py implementation under 300 lines and the unit test surface mockable through httpx.Client. | severity: cosmetic | blocking: aam-demo-experience.spec.js beforeAll hook needs 6+ min, not the default 60s
    RESOLVED 2026-05-14 (Plan C WP6, commit 359ee88) — moot. `app/ingest/dcl_pusher.py` deleted along with the rest of WP4's demo-ingest library. AAM has no DCLPusher anymore. If a future AAM code path needs to push to DCL at volume, port Farm's `farm/src/services/dcl_triple_pusher.py` directly — it's already the proven async-batched implementation.
13. 2026-05-15 | wp12-ipaas-gate | app/adapters/ipaas.py:50-80 + .env*  | WP12 (make AAM changes visible + real-path e2e Console demo) is gated on real Workato + Boomi sandbox accounts with deployed recipes/processes. Gate status as of 2026-05-15: FAILED. Missing env vars: WORKATO_API_TOKEN, WORKATO_BASE_URL, WORKATO_WEBHOOK_SECRET, BOOMI_ACCOUNT_ID, BOOMI_USERNAME, BOOMI_API_TOKEN, BOOMI_BASE_URL, BOOMI_ATOM_ID. Adapter code (`connect`, `check_health`, `discover_pipes`, `self_heal`) raises NotImplementedError for every vendor; the existing FinOps demo path runs through HARNESS_MODE=stub → local ipaas_stub, not real sandboxes. No test recipe (NetSuite vendor master + AP invoices via Workato) and no test process (Okta apps + users + assignments via Boomi) is deployed. Sandbox accounts have not been provisioned. WP12b (AAM UI surfaces for resolver/converter/push activity), WP12c (stage benchmarks), WP12d (Console real-path e2e demo), WP12e (AAM trigger affordance) all block on this — building them against stub data violates A1/A2 per WP12 prompt's explicit "Demo credibility requires real iPaaS calls, not local stubs". | severity: blocker | blocking: WP12 in full
    PARTIALLY RESOLVED 2026-05-15 (WP12a') — Farm now ships a contract-faithful Workato + Boomi sim pair under farm/src/fabric_sims/, version-pinned (Workato Platform API v2.0, Boomi AtomSphere REST v1) against curated OpenAPI snapshots + per-vendor fidelity checklists. AAM's IPaaSAdapter has real workato + boomi connect/check_health/discover_pipes/self_heal implementations speaking the vendor REST contracts; sim-vs-vendor is a WORKATO_BASE_URL / BOOMI_BASE_URL swap (no code change). New AAM webhook receivers /api/aam/webhooks/workato and /api/aam/webhooks/boomi verify HMAC-SHA256 against *_WEBHOOK_SECRET (401 on mismatch), run the WP3 record resolver, and POST WP4-shape triples to /api/dcl/ingest-triples. E2E smoke (farm/tests/fabric_sims/test_e2e_workato_boomi.py) is 7/7 green twice in a row, includes valid + invalid signature cases per vendor, and confirms the LinkedIn fuzzy pair lands in HITL at confidence 0.725. Real-vendor sandbox provisioning is no longer a gate for the demo path — but WP12b/c/d/e remain out of scope this pass. mulesoft / tray.io / zapier adapters still NotImplementedError (only workato + boomi were in WP12a' scope). Note: AAM's pm2 process loads `.env` (prod) not `.env.development` — sim env vars were added to `.env.development` only. Defaults match sim defaults so live triggering works; for any test pointing AAM at dev DCL (port 8104), reload pm2 with .env.development or set DCL_URL via env override.
    FURTHER RESOLVED 2026-05-15 (WP12b+e) — New AAM Fabrics tab at /aam/fabrics (server-rendered HTML, vanilla fetch + 5s setInterval). Per-vendor cards (workato, boomi, auto-driven by adapters/__init__.py:IMPLEMENTED_VENDORS) show: env-present check, IPaaSAdapter.check_health() result with latency, 24h aggregate counts (received/verified/push_succeeded/triples/errors), trigger button (proxies POST /api/aam/fabrics/{vendor}/trigger → Farm POST /farm/fabric-sims/trigger/{vendor}). Recent receipts table auto-refreshes every 5s; row click opens drill panel showing webhook payload + DCL ingest-status (triple_count + concept_summary fetched live via DCL HTTP — drill is multi-DB-correct since AAM and DCL run against different Supabase projects). Manual-entry section with pipe dropdown filtered to `_MANUAL_PIPE_TO_EVENT` (5 routable pipes, dynamic field inputs from MAPPINGS, runs same code path as webhook receivers, source='manual' on receipt). Migration: `migrations/add_fabric_webhook_log.sql` + entries added to `app/db/schema.py:_run_migrations()` for runtime application; `CREATE TABLE IF NOT EXISTS` so safe to re-run. WP12c (benchmarks), WP12d (Console e2e demo), additional fabrics (MuleSoft, Tray, Zapier, direct-to-app) remain out of scope.
14. 2026-05-15 | wp12a-prime | farm/src/fabric_sims/ + Farm UI | WP12c (stage benchmarks) flag from the WP12a' plan: the fabric sims default to `latency_profile: zero` in farm/config/fabric_sims.yaml. Honest e2e timing claims require flipping to `latency_profile: realistic` with synthetic delays modeled on public Workato/Boomi p50/p95. Build the latency-injection middleware in WP12c. Without it, benchmarks understate real-world latency by 10-100x. | severity: degraded | blocking: WP12c benchmark credibility
15. 2026-05-16 | wp12a-prime-cleanup | aam + farm | During WP12a'/WP12b+e, a process-wide AOS_ENTITY_ID env var with a hardcoded "finops-demo-co"-class default was used to stamp entity_id on every triple. The initial value chosen was a CLAUDE.md F1-banned tag, polluting 942 rows in DCL prod semantic_triples + 25 rows in AAM SQLite resolver_hitl_queue + 8 rows in AAM Postgres fabric_webhook_log. Hard-deleted all three populations. Architectural correction: removed AOS_ENTITY_ID entirely; replaced with per-sim config (sims.<vendor>.tenant_entity_id in farm/config/fabric_sims.yaml; sim startup fail-loud if missing) and per-adapter env (WORKATO_TENANT_ENTITY_ID + BOOMI_TENANT_ENTITY_ID; receiver returns 500 if unset). Manual entry now requires explicit entity_id in request body (400 if missing). Pre-commit hooks synced: Farm now checks meridian/cascadia like AAM, and both hooks now check .env.example. Two-case parametrized test proves per-sim binding flows distinct entity_id values end-to-end to DCL dev. The cleanup also exposed: AAM's pm2 .env had DCL_URL=:8004 (prod), which is how the polluted triples reached prod in the first place. .env.development now correctly points DCL at :8104. pm2 AAM reload with .env.development is required for any test runs going forward; out-of-scope for this dispatch. | severity: degraded | blocking: nothing (cleanup complete; dispatch-of-record for the meridian-saas pollution incident)
    RESOLVED 2026-05-16 (this dispatch) — see commits for aam + farm.
16. 2026-05-16 | ws-1-foundation | tests/playwright/aam-fabrics-*.spec.ts + dcl/backend/api/routes/triple_monitor.py:537 | The new Fabrics drill page surfaces per-receipt triples by calling DCL's `/api/dcl/triples/browse?run_id=<dcl_ingest_id>` — a new filter added in this session (additive, read-only, UUID-validated, returns 400 on malformed). dcl-dev (:8104) reloaded the change automatically (--reload); dcl-backend (:8004) was NOT restarted in this dispatch since the demo path runs dev-only. Next prod-touch session must `pm2 restart dcl-backend` after pulling, otherwise B2 drill against prod-DCL will return 0 triples even when triples exist. The `--reload` mode mismatch between dcl-dev (on) and dcl-backend (off) is per CLAUDE.md's two-process model and intentional. | severity: cosmetic | blocking: nothing today
17. 2026-05-16 | ws-1-foundation | nlq/src/nlq/api/dashboard_routes.py:32 — NLQ BLOCK 3 HALT | NLQ has no prebuilt CTO or cloud-cost-optimization dashboard schemas in code or DCL. _dashboard_cache is dead code (declared, never written). Dashboards are runtime-generated from NL queries by dashboard_generator.py:124 with random `dash_<8hex>` IDs. CTO persona routing exists (routes.py + dcl_semantic_client.py) but does not produce a stable dashboard surface. Block 3 of the WS-1 dispatch halted awaiting authorization for one of: (a) author stable cto + cloud-cost schemas in NLQ (requires tile spec from Ilya), (b) re-architect /api/v1/dashboard/{id} to read DashboardSessionStore (breaks "stable persona URL" promise), (c) clarify whether the dispatch's premise was wrong. Provenance gap is separate: dashboard_data_resolver.py:677 captures aggregated run_provenance but widget resolvers do not propagate per-triple {source_system, source_field, pipe_id, fabric_plane, confidence_score} — requires DCL contract change to return per-triple provenance in query responses. See nlq_deferred_work.md#19 for the discovery transcript. | severity: blocker | blocking: WS-1 Block 3 + Block 8.6 (nlq-dashboards.spec.ts)
    REDIRECTED 2026-05-16 (Ilya, WS-1 closeout) — Block 3 closed as architecture-premise correction; no schema authoring authorized. Per deck Slide 11 the named-dashboard model is runtime-generated ("user asks the question, NLQ renders as dashboard with drill-through"); the runtime path already works for dashboard rendering. No prebuilt `cto` / `cloud-cost-optimization` schemas needed. `/api/v1/dashboard/{id}` and `_dashboard_cache` remain dead code today and can be deleted as a future NLQ cleanup chore — not on the WS-1 critical path. The per-triple provenance gap is reclassified as a **WS-5 (AOS-MCP Server) prerequisite**, not a tactical WS-1 fix. AOS-MCP Server defines the serving contract for every consumer (FinOps agent, AOS Intelligence dashboard drill-through, simulated BI federation), so provenance must surface end-to-end. Cross-module scope of the WS-5 prerequisite: (1) **DCL** — `/api/dcl/triples/browse` and every equivalent query endpoint must return per-triple provenance in the response shape (source_system, source_field, pipe_id, fabric_plane, confidence_score). Fields already exist on each row; the WS-1 `run_id` filter case already returns them. Audit every other DCL query endpoint to confirm shape parity. (2) **NLQ** — `dashboard_data_resolver.py` widget resolvers (`_resolve_kpi_data`, `_resolve_time_series_data`, etc.) must thread per-triple provenance from the resolver layer onto every widget return dict; today `_provenance` is captured at resolver scope but dropped before reaching widget data. (3) **AOS-MCP Server** — response envelope must surface provenance to consumers; envelope shape pending in WS-5. This entry stays open as the WS-5 entry point for the gap.
18. 2026-05-16 | ws-1-foundation | Block 6 dispatch premise mismatch | WS-1 dispatch said "FinOps :8007 process down". Per aos-launch.sh:23, FinOps is on port 5000 (Node/Express, up at PID 6907 with ~16h uptime) and port 8007 is RevOps (Python/FastAPI, down). FinOps standalone responds /health 200 with 154 recommendations; AOS-aware FinOps :5001 is up but DCL MCP returns 401 (separate auth issue, finops_deferred_work.md#11). RevOps :8007 outage is a real demo-stack failure but out of the WS-1 scope as written. Either: (a) re-dispatch as "Restore RevOps on 8007" against /home/ilyac/code/revops/api.py, or (b) treat B6 as already-satisfied (FinOps is up). Block 6 deferred pending direction. | severity: degraded | blocking: nothing in WS-1 (FinOps up)
    DEFERRED 2026-05-16 (Ilya, WS-1 closeout) — RevOps :8007 deferred to a future ops-pass workstream; NOT bundled into WS-2 (Sage Intacct + dataset rework). RevOps is not on the deck demo path. Diagnosis: /home/ilyac/code/revops/ has no `.venv` directory — `revops|$BASE/revops|${VENV} python3 -m uvicorn api:app --host 0.0.0.0 --port 8007 --reload|8007|frontend|3007` (aos-launch.sh:29) cannot start because $VENV resolves to a missing path. Restoration requires: (i) create .venv via `python3 -m venv .venv`, (ii) `pip install -r requirements.txt` (verify file exists at revops repo root), (iii) start via aos-launch.sh or pm2 with the launch command above, (iv) confirm `curl :8007/health` returns 200. Tag: ops-pass workstream, post-WS-2.
19. 2026-05-16 | ws-1-foundation | platform/tests/test_3h_mai.py:22 | TENANT_ID = os.environ.get("TENANT_ID", "400aa910-a6b4-5d44-ab9f-e6aecde37721"). Hardcoded F1-banned UUID as test fallback default. Platform's pre-commit hook exempts tests/* for entity names (meridian/cascadia) but NOT for hex UUIDs — any future modification of this file will fail commit. Default UUID matches one of the seeded UUIDs the WP12a'-cleanup hard-deleted from prod yesterday; running pytest against prod-DB would re-introduce it. Surfaced by WS-1 Block 5 cross-repo scrub. See platform_deferred_work.md#13. Fix path: raise on unset TENANT_ID env (A1 compliance) or generate UUID4 per session. | severity: degraded | blocking: any modification of test_3h_mai.py + pytest hygiene against prod RESOLVED 2026-05-16 (commit platform 9ea73b3) — UUID rotated to c2a90318-... per WS-2 B0. See platform_deferred_work.md#13.
